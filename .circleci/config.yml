version: 2
jobs:
  build:
    machine:
      image: circleci/classic:latest
    environment:
      CI: 1
    steps:
      - checkout
      # - run: |
      #     circleci-install nodejs 10.6.0
      #     sudo -H -i -u ${CIRCLECI_USER} nvm alias default 10.6.0
      - run: sudo -H -i -u ${CIRCLE_PROJECT_USERNAME} nvm
      - run: node --version
