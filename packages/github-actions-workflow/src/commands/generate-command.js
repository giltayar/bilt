/**
 *
 * @param {{'template-workflow-file': string}} options
 */
export async function generateCommand({'template-workflow-file': templateWorkflowFile}) {
  await 1
  console.log(`${templateWorkflowFile}`)
}
