'use strict'
const {describe, it} = require('mocha')
const {expect} = require('chai')

describe('noop-repository-fetcher', function() {
  it('should work', async () => {
    expect(4).to.equal(4)
  })
})
